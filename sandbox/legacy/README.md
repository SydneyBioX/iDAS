# Legacy iDAS implementation

This directory preserves earlier iDAS implementations for development
reference. The files are excluded from package builds, are not part of the
supported API, and should not be sourced by users.

Use `iDAS()` and the current implementation under `R/` for all new analyses.

The `man/` subdirectory contains the generated documentation that accompanied
the legacy functions.
